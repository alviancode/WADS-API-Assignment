/*const mongoose = require('mongoose')
const { ObjectId } = mongoose.Schema.Types
const venieSchema = new mongoose.Schema({
    venue: String,
    address: String
})

mongoose.model("Post", postSchema)
*/